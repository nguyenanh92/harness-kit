interface:
  display_name: "Harness Kit"
  short_description: "Scaffold and validate standard-library-only Python agent harnesses"
  default_prompt: "Use $harness-kit to scaffold, validate, or audit a zero-dependency Python Agent Harness for this repository."

policy:
  allow_implicit_invocation: true
